<% provide(:title, '注册') %>
<h1>注册一个管理员用户</h1>
<div class="row">
  <div class="col-md-6 col-md-offset-3">
    <%= form_for(@user) do |f| %>
      <%= render 'fields', f: f %>
      <%= f.submit "创建帐号", class: "btn btn-primary" %>
    <% end %>
  </div>
</div>
